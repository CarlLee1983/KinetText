---
phase: 05-testing-publishing
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - src/tests/e2e/setup.ts
  - src/tests/e2e/fixtures.ts
  - src/tests/e2e/utils.ts
  - src/tests/e2e/AudioConversion.e2e.ts
  - src/tests/e2e/AudioMerging.e2e.ts
autonomous: true
requirements: [R1.1.1, R1.1.2, R1.2.1, R1.2.2, R1.2.3]

must_haves:
  truths:
    - "Phase 2-3 的 E2E 測試完整驗證轉換與合併"
    - "測試基礎設施可重用於後續測試"
    - "生成的 MP3 檔案驗證完整"
  artifacts:
    - path: "src/tests/e2e/"
      provides: "Phase 2-3 E2E 測試基礎設施與測試"
      min_lines: 800
  key_links:
    - from: "E2E 基礎設施"
      to: "Phase 2-3 測試"
      via: "setup, fixtures, utils 支援"
      pattern: "setup.ts, fixtures.ts"
---

<objective>
建立 E2E 測試基礎設施，實現 Phase 2-3 的完整端到端驗證（音頻轉換與合併）。

**目的**: 為 Phase 1-4 的後續 E2E 測試奠定基礎，驗證轉換與合併功能
**輸出**:
- E2E 測試基礎設施 (setup, fixtures, utils)
- Phase 2 音頻轉換 E2E 測試
- Phase 3 音頻合併 E2E 測試
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

## Phase 2-3 E2E 測試範圍

```
Phase 2: 音頻轉換
  ├─ 單檔轉換 (WAV → MP3)
  ├─ 多格式轉換 (AAC, OGG, FLAC)
  ├─ 轉換失敗重試
  └─ 性能基準 (轉換時間)

Phase 3: 音頻合併
  ├─ 批量合併 (10+ 檔案)
  ├─ 時長計算驗證
  ├─ 分組結果驗證
  └─ 性能基準 (合併時間)
```

## 現有測試資源

- 374+ 單元測試已通過 (Wave 1 - 05-01)
- 樣本音頻檔案應在 tests/ 目錄可用
- music-metadata 已安裝，可用於驗證
</context>

<tasks>

<task type="auto">
  <name>Task 1: 建立 E2E 測試基礎設施 (setup, fixtures, utils)</name>
  <files>
    src/tests/e2e/setup.ts
    src/tests/e2e/fixtures.ts
    src/tests/e2e/utils.ts
  </files>
  <action>
  建立所有 E2E 測試共用的基礎設施：

  **src/tests/e2e/setup.ts** (100 行):
  - beforeAll: 初始化測試環境 (臨時目錄、FFmpeg 檢查)
  - afterAll: 清理臨時檔案
  - 日誌記錄初始化

  **src/tests/e2e/fixtures.ts** (150 行):
  - getSampleWAV(): 提供或建立樣本 WAV 檔案
  - getSample10MP3s(): 提供 10 個測試 MP3 檔案
  - 管理測試檔案生命週期

  **src/tests/e2e/utils.ts** (100 行):
  - verifyMP3File(path): 驗證 MP3 存在且可讀
  - getMp3Duration(path): 使用 music-metadata 取得時長
  - cleanupTestFiles(dir): 清理測試檔案

  遵循 Bun test 風格，函數 < 50 行。
  </action>
  <verify>
    <automated>test -f src/tests/e2e/setup.ts && test -f src/tests/e2e/fixtures.ts && test -f src/tests/e2e/utils.ts</automated>
  </verify>
  <done>E2E 基礎設施完成，測試樣本管理就緒</done>
</task>

<task type="auto">
  <name>Task 2: Phase 2 E2E 測試 - 音頻轉換</name>
  <files>src/tests/e2e/AudioConversion.e2e.ts</files>
  <action>
  建立 Phase 2 音頻轉換的 E2E 測試 (200+ 行)：

  **測試情景**:
  1. **單檔轉換** (WAV → MP3)
     - 輸入: 樣本 WAV
     - 驗證: 輸出 MP3 存在、可讀、有效

  2. **多格式轉換** (AAC, OGG, FLAC → MP3)
     - 依序轉換 4 個不同格式
     - 驗證所有輸出都是有效的 MP3

  3. **轉換失敗重試**
     - 確認 RetryService 在失敗時重試
     - 驗證最終成功或失敗的行為

  4. **性能基準**
     - 測量轉換時間
     - 驗證時間在合理範圍內

  測試結構使用 beforeAll/afterAll hook 管理生命週期。
  </action>
  <verify>
    <automated>test -f src/tests/e2e/AudioConversion.e2e.ts && bun test src/tests/e2e/AudioConversion.e2e.ts 2>&1 | grep -E "pass|fail"</automated>
  </verify>
  <done>Phase 2 E2E 測試完成，音頻轉換驗證通過</done>
</task>

<task type="auto">
  <name>Task 3: Phase 3 E2E 測試 - 音頻合併</name>
  <files>src/tests/e2e/AudioMerging.e2e.ts</files>
  <action>
  建立 Phase 3 音頻合併的 E2E 測試 (250+ 行)：

  **測試情景**:
  1. **批量合併** (mergeBatch)
     - 輸入: 10+ 個 MP3 檔案
     - 驗證: GroupingReport 結構完整

  2. **時長計算與驗證**
     - 驗證 DurationService 計算準確
     - 驗證後合併驗證 (actualDuration)
     - 檢查誤差 < 1%

  3. **分組結果驗證**
     - 確認分組數量合理
     - 每組時長接近目標 (容差 ±10%)

  4. **性能基準**
     - 測量合併時間 (10 個檔案)
     - 驗證在 5 分鐘內完成

  完整的 mergeBatch 工作流驗證。
  </action>
  <verify>
    <automated>test -f src/tests/e2e/AudioMerging.e2e.ts && bun test src/tests/e2e/AudioMerging.e2e.ts 2>&1 | grep -E "pass|fail"</automated>
  </verify>
  <done>Phase 3 E2E 測試完成，音頻合併驗證通過</done>
</task>

</tasks>

<verification>
E2E 測試完整性檢查清單：

1. **基礎設施**
   - [x] setup.ts 初始化和清理
   - [x] fixtures.ts 樣本檔案管理
   - [x] utils.ts 驗證函式

2. **Phase 2 E2E**
   - [x] 單檔轉換測試
   - [x] 多格式轉換測試
   - [x] 重試邏輯測試
   - [x] 性能基準測試

3. **Phase 3 E2E**
   - [x] 批量合併測試
   - [x] 時長驗證測試
   - [x] 分組驗證測試
   - [x] 性能基準測試
</verification>

<success_criteria>
- [x] E2E 基礎設施完成
- [x] Phase 2 轉換 E2E 測試通過
- [x] Phase 3 合併 E2E 測試通過
- [x] 所有測試 < 5 分鐘執行時間
</success_criteria>

<output>
完成後，建立:
- `src/tests/e2e/setup.ts`
- `src/tests/e2e/fixtures.ts`
- `src/tests/e2e/utils.ts`
- `src/tests/e2e/AudioConversion.e2e.ts`
- `src/tests/e2e/AudioMerging.e2e.ts`
- `.planning/phases/05-testing-publishing/05-02-SUMMARY.md`

並提交 git：
```
git add -A
git commit -m "test(05-02): add E2E infrastructure and Phase 2-3 tests"
```
</output>
