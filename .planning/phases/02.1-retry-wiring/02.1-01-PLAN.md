# Plan 02.1-01: Wire AudioErrorClassifier into RetryService

**Wave:** 1
**Autonomous:** true
**Depends on:** 02-02, 02-03
**Requirements addressed:** N/A (gap closure)
**Files modified:**
- `src/Retry/RetryService.ts` — Add errorClassifier parameter
- `src/core/services/AudioConvertService.ts` — Inject AudioErrorClassifier
- `src/core/services/AudioMergeService.ts` — Inject AudioErrorClassifier
- `src/tests/unit/RetryService.test.ts` — Add classifier injection test
- `src/tests/integration/AudioConversion.test.ts` — Verify FFmpeg error classification

**Objective:** Enable intelligent FFmpeg error classification during retry decisions. Currently AudioErrorClassifier is defined but unused. This fix wires the classifier into RetryService so that FFmpeg-specific errors (unsupported codec, allocation failures, etc.) trigger correct retry/fail behavior.

---

## Task 1: Modify RetryService to accept errorClassifier parameter

**Description:** Add optional `errorClassifier` parameter to RetryService constructor, allowing dependency injection of custom classifiers.

**Read first:**
- `src/Retry/RetryService.ts` (current implementation)
- `src/core/services/AudioErrorClassifier.ts` (AudioErrorClassifier extends ErrorClassifier)

**Action:**
1. Open `src/Retry/RetryService.ts`
2. In constructor signature, add optional parameter after `config`:
   ```typescript
   constructor(
     config: RetryConfig,
     errorClassifier?: ErrorClassifier
   )
   ```
3. Update constructor body to use injected classifier if provided, otherwise default to base ErrorClassifier:
   ```typescript
   this.errorClassifier = errorClassifier ?? new ErrorClassifier()
   ```
4. Update class-level type: ensure `errorClassifier: ErrorClassifier` property is present

**Acceptance criteria:**
- `bun test src/tests/unit/RetryService.test.ts` includes test "accepts custom errorClassifier in constructor"
- Test verifies that when `new RetryService(config, new AudioErrorClassifier())` is called, the instance uses the injected classifier
- No breaking changes to existing RetryService callers (backward compatible)

**Done:** Commit with message `feat(retry): Add errorClassifier dependency injection`

---

## Task 2: Inject AudioErrorClassifier into AudioConvertService and AudioMergeService

**Description:** Update both services to create and pass AudioErrorClassifier when instantiating RetryService.

**Read first:**
- `src/core/services/AudioConvertService.ts` (check where RetryService is created)
- `src/core/services/AudioMergeService.ts` (check where RetryService is created)
- `src/core/services/AudioErrorClassifier.ts` (verify import path)

**Action:**
1. In `AudioConvertService.ts`:
   - Find line where `this.retryService = new RetryService(...)`
   - Update to: `this.retryService = new RetryService(this.config, new AudioErrorClassifier())`
   - Verify import: `import { AudioErrorClassifier } from './AudioErrorClassifier'` at top of file

2. In `AudioMergeService.ts`:
   - Find line where `this.retryService = new RetryService(...)`
   - Update to: `this.retryService = new RetryService(this.config, new AudioErrorClassifier())`
   - Verify import: `import { AudioErrorClassifier } from './AudioErrorClassifier'` at top of file

3. Run tests to verify no regressions:
   ```bash
   bun test src/tests/unit/AudioConvertService.test.ts
   bun test src/tests/unit/AudioMergeService.test.ts
   bun test src/tests/integration/AudioConversion.test.ts
   ```

**Acceptance criteria:**
- `src/core/services/AudioConvertService.ts` contains: `new RetryService(this.config, new AudioErrorClassifier())`
- `src/core/services/AudioMergeService.ts` contains: `new RetryService(this.config, new AudioErrorClassifier())`
- Both files have correct import: `import { AudioErrorClassifier } from './AudioErrorClassifier'`
- All unit tests pass (AudioConvertService, AudioMergeService)
- Integration tests for AudioConversion pass (WAV/AAC/OGG/FLAC → MP3)
- No new console.log statements (use pino logger for any new logging)

**Done:** Commit with message `feat(audio): Inject AudioErrorClassifier into RetryService for FFmpeg error classification`

---

## Verification

After both tasks complete, run full test suite to confirm:
```bash
bun test src/tests/unit/
bun test src/tests/integration/
```

Expected outcome: All tests pass, FFmpeg conversion and merge operations now use intelligent error classification during retries.

Must-haves:
- AudioErrorClassifier wired into RetryService ✓
- FFmpeg errors correctly classified (fail vs retry) ✓
- No test regressions ✓
- Backward compatible ✓
