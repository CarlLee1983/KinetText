# KinetiText

KinetiText 將小說擷取、文字轉語音與媒體輸出串成可選擇的使用者工作流程。本詞彙表定義這些流程在可靠性與診斷上的共同語言。

## Language

**工作流程設定檔**：
一組對應單一使用者流程的必要條件與可用能力，用於在執行前評估該流程是否可進行；設定檔本身不執行流程。
_Avoid_: 全域健康檢查、指令清單

**Go 輔助工具**：
為特定媒體能力提供增強效果的選用 KinetiText 輔助程式。它不健康時，核心流程仍應以既有替代能力維持可用。
_Avoid_: 必要依賴、核心服務

**階段產物**：
由單一工作流程階段產生、可供後續階段使用的輸出。只有其來源追溯與目前輸入、設定及產生版本相符時，才能在續作中重用。
_Avoid_: 快取檔、任意既有檔案

**來源追溯清單**：
用來說明階段產物由哪些輸入與條件產生的持久紀錄。它是判定階段產物是否仍有效的依據。
_Avoid_: 檔案存在檢查、執行日誌

**舊版產物**：
缺少來源追溯清單的既有階段產物。它會被保留，但在重新產生並建立可驗證的來源追溯前，不得自動重用。
_Avoid_: 已驗證產物、可續作產物

**作品工作流程狀態清單**：
隨單一作品保存的持久紀錄，集中登錄其工作流程階段、階段產物與來源追溯。它是該作品續作狀態的唯一依據。
_Avoid_: 全域索引、產物側錄檔

**階段發佈**：
將已完成並驗證的階段產物與其來源追溯納入作品工作流程狀態清單的原子步驟。只有完成階段發佈的產物才是有效產物。
_Avoid_: 直接寫入、半成品

**來源追溯指紋**：
用於比對階段產物是否仍對應相同內容與條件的穩定識別。它由關鍵輸入內容、正規化設定與適用的外部工具版本組成。
_Avoid_: 檔案時間戳、檔案路徑

**階段產生器版本**：
描述單一工作流程階段產物契約的版本。當此版本變更時，該階段及其下游產物需要重新產生；輸入、設定或適用工具的變更亦同。
_Avoid_: 全域應用程式版本

**階段依賴關係**：
工作流程階段間明確的輸入關係。一個階段失效時，所有依賴它的下游階段也隨之失效。
_Avoid_: 全流程重做、手動猜測

**祕密設定指紋**：
不保存原始祕密值、僅用於判斷相關設定是否變更的不可逆識別。它可出現在來源追溯中，但不得在診斷或輸出中揭露祕密。
_Avoid_: 明文祕密、可讀取設定值

**網站適配器前置條件**：
單一網站適配器執行所需的本機能力。它只在網址解析到該適配器後，才成為爬取設定檔的檢查項目。
_Avoid_: 全部適配器的共同阻斷項

**備份遠端設定**：
供備份流程選擇目標位置的具名本機設定。診斷只確認其存在與可辨識性，不提前驗證遠端連線或權限。
_Avoid_: 遠端健康檢查、憑證內容
